<!DOCTYPE html>
<html>
<head>
  <title>CS4152Calories</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%# These classes come from the Bootstrap CSS framework. %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Calories Manager</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
<!--          <a class="nav-link active" aria-current="page" href="#">Home</a>-->
          <%= link_to 'Home', new_session_path, :class => 'nav-link active'%>
          <% if session[:user_uid] %>
            <%#= link_to "#{@user.first_name}, Logout", session_path(@user), method: 'delete', :class => 'nav-link' %>
            <%= link_to "#{User.find(session[:user_uid]).first_name}, Logout", session_path(session[:user_uid]), method: 'delete', :class => 'nav-link' %>

          <% else %>
            <%= link_to 'Login', new_session_path, :class => 'nav-link' %>
          <% end -%>
<!--          <a class="nav-link" href="#">Features</a>-->
<!--          <a class="nav-link" href="#">Pricing</a>-->
          <%= link_to 'Register', new_user_path, :class => 'nav-link' %>
        </div>
      </div>
    </div>
  </nav>
  <div class="container">
    <%# Render "flash" messages in our application. This loads _flash.erb %>
    <%= render 'layouts/flash' %>
    <main id="main">
      <%= yield %>
    </main>
  </div>
</body>
</html>
